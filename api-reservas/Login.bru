meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  { "email":"fbarroumeres@gmail.com", "password":"1234556" }
}

tests {
  bru.setVar("TOKEN", res.body.data.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
