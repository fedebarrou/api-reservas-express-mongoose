meta {
  name: Eliminar Reserva
  type: http
  seq: 9
}

delete {
  url: {{BASE_URL}}/reservations/{{RESERVATION_ID}}
  body: none
  auth: bearer
}

headers {
  Authorization: Bearer {{TOKEN}}
}

auth:bearer {
  token: {{TOKEN}}
}

vars:pre-request {
  RESERVATION_ID: 699cf2e91ddba64e7ec2940d
}

settings {
  encodeUrl: true
  timeout: 0
}
